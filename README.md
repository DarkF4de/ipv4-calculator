# ipv4-calculator
A simple, low level CLI (Command Line Interface) tool that calculates information about the User's IPv4 address without any help of any libraries. Depending on user input, it can calculate information such as:
1. Subnet Mask
2. Class
3. CIDR (Classless Inter-Domain Routing)
4. Network address
5. Broadcast address
